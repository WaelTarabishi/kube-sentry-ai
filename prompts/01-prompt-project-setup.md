# Project setup

Create the foundation for an on-demand AI Kubernetes troubleshooting agent using a FastAPI backend, a Next.js frontend, and Docker Compose.

The setup includes modular placeholders, environment templates, logging, CORS, and a health endpoint. It intentionally excludes Kubernetes commands, AI reasoning, provider integrations, authentication, and real-time updates.

